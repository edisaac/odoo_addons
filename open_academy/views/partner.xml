<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <record model="ir.ui.view" id="view_partner_form">
            <field name="name">res.partner.form.inherit.openacademy</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <!-- buscando un campo en especifico dentro de un tag-->
                <xpath expr="//group/field[@name='supplier']" position="after">
                    <field name="is_instructor"/>
                </xpath>
                <!-- buscando un campo en tag en especifico-->
                <xpath expr="//notebook" position="inside" >
                    <!--  muestra session solo si is instructor = si-->
                    <page string="Sessions" attrs="{'invisible':[('is_instructor','=',False)]}">
                        <field name="session_ids"/>
                    </page>
                </xpath>
            </field>
        </record>
        <record id="partner_action" model="ir.actions.act_window">
            <field name="name">Partner</field>
            <field name="res_model">res.partner</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>
    </data>
</openerp>